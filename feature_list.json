{
  "schema_version": 1,
  "features": [
    {
      "id": "F001",
      "title": "[DEMO] Bootstrap Python application repository",
      "behavior": "С нуля создаётся структура проекта: backend/ для production-кода, tests/unit/, tests/integration/, tests/e2e/, scripts/, pyproject.toml, docker-compose.yml и docs/. В backend/ создаётся FastAPI application factory. GET /health/live работает без внешних сервисов.",
      "state": "pass",
      "depends_on": [],
      "verification": "pytest -q tests/unit/test_app_bootstrap.py",
      "evidence": "uv run --frozen pytest -q tests/unit/test_app_bootstrap.py: 2 passed; docker compose config: passed"
    },
    {
      "id": "F002",
      "title": "[DEMO] Establish verification pipeline",
      "behavior": "Создаются scripts/verify-unit.sh, scripts/verify-integration.sh, scripts/verify-e2e.sh и scripts/verify.sh. Pytest markers разделяют unit/integration/e2e. Скрипты корректно возвращают ненулевой exit code при падении проверки.",
      "state": "pass",
      "depends_on": [
        "F001"
      ],
      "verification": "./scripts/verify-unit.sh",
      "evidence": "./scripts/verify-unit.sh: 17 passed; ./scripts/verify.sh: exit 0; empty suites and genuine failure propagation independently verified"
    },
    {
      "id": "F003",
      "title": "[DEMO] Add Ruff and gradual mypy checks",
      "behavior": "В pyproject.toml добавляются Ruff и mypy в gradual/non-strict режиме. Создаются scripts/lint.sh и scripts/typecheck.sh. Общий verification pipeline запускает lint, format check, type-check и тесты.",
      "state": "pass",
      "depends_on": [
        "F002"
      ],
      "verification": "./scripts/lint.sh && ./scripts/typecheck.sh",
      "evidence": "./scripts/lint.sh && ./scripts/typecheck.sh: pass; ./scripts/verify.sh: 27 unit tests and all quality/test stages passed"
    },
    {
      "id": "F004",
      "title": "[DEMO] Implement typed application configuration",
      "behavior": "Добавляются typed settings для environment, trusted single-owner runtime, PostgreSQL, MinIO, Qdrant, Neo4j, LightRAG, embedding endpoint, external LLM и logging. User identity и credential validation не являются частью конфигурации. Секреты не получают небезопасных production defaults.",
      "state": "pass",
      "depends_on": [
        "F003"
      ],
      "verification": "pytest -q tests/unit/config",
      "evidence": "pytest -q tests/unit/config: 23 passed; full verification pipeline: 50 unit tests, Ruff and mypy passed"
    },
    {
      "id": "F005",
      "title": "[DEMO] Add structured operational logging",
      "behavior": "Добавляется structured JSON logging с request_id, service, workspace_id, document_id, operation, duration_ms и result. Секреты, Authorization, document content, embeddings и полные prompts/responses не логируются.",
      "state": "pass",
      "depends_on": [
        "F004"
      ],
      "verification": "pytest -q tests/unit/observability/test_logging.py",
      "evidence": "pytest -q tests/unit/observability/test_logging.py: 16 passed; adversarial disclosure probes passed; full pipeline: 66 unit tests"
    },
    {
      "id": "F006",
      "title": "[DEMO] Bootstrap PostgreSQL in Docker Compose",
      "behavior": "В docker-compose.yml добавляются rag-api и PostgreSQL с healthcheck, named volume и internal network. Приложение получает readiness dependency на PostgreSQL. Проверяется persistence после restart.",
      "state": "pass",
      "depends_on": [
        "F004"
      ],
      "verification": "pytest -q tests/integration/test_postgres_bootstrap.py",
      "evidence": "uv run --frozen pytest -q tests/integration/test_postgres_bootstrap.py: 1 passed on real Docker Compose; readiness 200/503 and liveness independence verified; PostgreSQL row persisted after force-recreate; ./scripts/verify.sh: 68 unit and 1 integration passed"
    },
    {
      "id": "F007",
      "title": "[DEMO] Introduce application database migrations",
      "behavior": "Добавляется Alembic или эквивалентный механизм application migrations. Создаётся app schema и versioned migration history. Повторный upgrade актуальной БД идемпотентен.",
      "state": "pass",
      "depends_on": [
        "F006"
      ],
      "verification": "pytest -q tests/integration/test_migrations.py",
      "evidence": "pytest -q tests/integration/test_migrations.py: 1 passed on real PostgreSQL; fresh upgrade created graph_blizz schema at revision 20260917_0001; repeated upgrade preserved the schema/version snapshot; ./scripts/verify.sh passed with 68 unit and 2 integration tests"
    },
    {
      "id": "F008",
      "title": "[DEMO] Persist workspaces",
      "behavior": "Создаётся минимальная таблица workspaces: UUID id, name, slug, immutable server-generated storage_key, status и timestamps. Rename не меняет storage_key. Repository не зависит от authentication tables.",
      "state": "pass",
      "depends_on": [
        "F007"
      ],
      "verification": "pytest -q tests/unit/workspaces/test_repository.py tests/integration/test_workspace_schema.py",
      "evidence": "uv run --frozen pytest -q tests/unit/workspaces/test_repository.py: 4 passed; PostgreSQL integration tests for workspace schema and migrations: 3 passed; ./scripts/verify.sh passed lint, format, mypy and 72 unit tests"
    },
    {
      "id": "F009",
      "title": "[DEMO] Establish trusted request context without authentication",
      "behavior": "С нуля определяется внутренний trusted request/workspace context, чтобы domain services не зависели от HTTP credentials. Реализуется единственный bootstrap owner context для совместимости текущего кода; Authorization header, IdP, user tables, OIDC, RBAC и ACL не требуются и не добавляются.",
      "state": "pass",
      "depends_on": [
        "F004",
        "F008"
      ],
      "verification": "pytest -q tests/unit/security/test_demo_owner_mode.py tests/integration/test_demo_owner_http.py",
      "evidence": "uv run --frozen pytest -q tests/unit/security/test_demo_owner_mode.py tests/integration/test_demo_owner_http.py: 16 passed; independent runtime/static audit confirmed immutable contexts, all 12 Demo/MVP permissions, credential-free HTTP demo_owner wiring and no OIDC/RBAC/ACL dependency; ./scripts/verify.sh passed lint, format, mypy, 86 unit tests and 2 integration tests"
    },
    {
      "id": "F010",
      "title": "[DEMO] Expose unauthenticated workspace API",
      "behavior": "Реализуются POST/GET workspace endpoints в доверенном single-owner runtime. Они работают без JWT, users, credentials и workspace_members; storage_key никогда не принимается от клиента и не раскрывается как mutable field.",
      "state": "pass",
      "depends_on": [
        "F008",
        "F009"
      ],
      "verification": "pytest -q tests/unit/api/test_workspaces_demo.py tests/integration/test_workspace_api_demo.py",
      "evidence": "uv run --frozen pytest -q tests/unit/api/test_workspaces_demo.py tests/integration/test_workspace_api_demo.py: 5 passed; independent runtime/static audit confirmed POST/GET behavior, DemoOwner permission enforcement, credential-free operation, rejection and non-disclosure of storage_key; ./scripts/verify.sh passed lint, format, mypy, 90 unit tests and 3 integration tests"
    },
    {
      "id": "F011",
      "title": "[DEMO] Bootstrap MinIO object storage",
      "behavior": "В Docker Compose добавляется MinIO с persistent volume и healthcheck. Реализуется минимальный ObjectStorage adapter put/get/delete. Проверяется persistence после restart.",
      "state": "pass",
      "depends_on": [
        "F006"
      ],
      "verification": "pytest -q tests/unit/storage/test_object_store.py tests/integration/test_minio.py",
      "evidence": "UV_CACHE_DIR=/tmp/graph-rag-uv-cache uv run --frozen pytest -q tests/unit/storage/test_object_store.py tests/integration/test_minio.py: 7 passed on real MinIO; persistence survived stop and force-recreate with the named volume; ./scripts/verify.sh passed lint, format, mypy, 96 unit tests and 8 integration tests"
    },
    {
      "id": "F012",
      "title": "[DEMO] Persist minimal document registry",
      "behavior": "Создаётся таблица documents: id, workspace_id, source_key, filename, source_type, object_uri, content_hash, status, timestamps. Статусы Demo: UPLOADED, INDEXING, READY, FAILED. Revisions и update/delete пока не реализуются.",
      "state": "pass",
      "depends_on": [
        "F007",
        "F008"
      ],
      "verification": "pytest -q tests/unit/documents/test_registry.py tests/integration/test_document_schema.py",
      "evidence": "UV_CACHE_DIR=/tmp/graph-rag-uv-cache uv run --frozen pytest -q tests/unit/documents/test_registry.py tests/integration/test_document_schema.py: 6 passed on real PostgreSQL; workspace-scoped create/get/list, schema constraints and four Demo statuses independently verified; ./scripts/verify.sh passed lint, format, mypy, 100 unit tests and 10 integration tests"
    },
    {
      "id": "F013",
      "title": "[DEMO] Store original document source",
      "behavior": "DocumentSourceService сохраняет original source в MinIO до indexing и связывает object_uri с document metadata. При indexing failure source остаётся доступен. Используется простой Demo key без revision segment.",
      "state": "pass",
      "depends_on": [
        "F011",
        "F012"
      ],
      "verification": "pytest -q tests/unit/documents/test_source_storage.py tests/integration/test_document_source_storage.py",
      "evidence": "UV_CACHE_DIR=/tmp/graph-rag-uv-cache uv run --frozen pytest -q tests/unit/documents/test_source_storage.py tests/integration/test_document_source_storage.py: 3 passed on real MinIO/PostgreSQL; exact bytes persisted before indexing and remained retrievable after downstream failure, with correct metadata/hash/status and revision-free workspace/document key; ./scripts/verify.sh passed lint, format, mypy, 104 unit tests and 11 integration tests"
    },
    {
      "id": "F014",
      "title": "[DEMO] Add basic parser abstraction",
      "behavior": "Создаются DocumentParser, ParsedDocument, ParsedChunk и parser registry. Полноценно поддерживаются plain text и Markdown. Текстовые исходники Python/JS/TS/Java/Go на Demo могут обрабатываться PlainTextParser. Tree-sitter не является dependency Demo.",
      "state": "pass",
      "depends_on": [
        "F003"
      ],
      "verification": "pytest -q tests/unit/parsers/test_registry.py tests/unit/parsers/test_text_parser.py",
      "evidence": "UV_CACHE_DIR=/tmp/graph-rag-uv-cache uv run --frozen pytest -q tests/unit/parsers/test_registry.py tests/unit/parsers/test_text_parser.py: 37 passed; independent adversarial verification covered immutable validated models, LF/CRLF Markdown fences and ATX headings, deterministic registry mappings and unsupported types; ./scripts/verify.sh passed lint, format, mypy, 147 unit tests and integration suite (3 passed, 8 skipped)"
    },
    {
      "id": "F015",
      "title": "[DEMO] Bootstrap Qdrant",
      "behavior": "В Docker Compose добавляется Qdrant с healthcheck и persistent volume. Реализуется connectivity adapter. Прямой пользовательский API к Qdrant отсутствует. Qdrant доступен в вебе.",
      "state": "pass",
      "depends_on": [
        "F006"
      ],
      "verification": "pytest -q tests/integration/test_qdrant_bootstrap.py",
      "evidence": "UV_CACHE_DIR=/tmp/graph-rag-uv-cache uv run --frozen pytest -q tests/integration/test_qdrant_bootstrap.py: 1 passed on real Qdrant; connectivity and named-volume persistence survived forced container recreation; independent verification confirmed HTTP /readyz healthcheck requiring status 200, configurable web access, internal adapter lifecycle/error mapping and absence of a public application route; ./scripts/verify.sh passed lint, format, mypy, 151 unit tests and 12 integration tests"
    },
    {
      "id": "F016",
      "title": "[DEMO] Bootstrap Neo4j",
      "behavior": "В Docker Compose добавляется Neo4j с healthcheck и persistent volume. Проверяются connection и persistence. Neo4j не является публичной application boundary.",
      "state": "pass",
      "depends_on": [
        "F006"
      ],
      "verification": "pytest -q tests/integration/test_neo4j_bootstrap.py",
      "evidence": "UV_CACHE_DIR=/tmp/graph-rag-uv-cache uv run --frozen pytest -q tests/integration/test_neo4j_bootstrap.py: 1 passed on real Neo4j; authenticated Bolt connection and named-volume persistence survived forced container recreation; independent verification confirmed authenticated cypher-shell readiness, internal-only network boundary, adapter lifecycle/error mapping and absence of a public application route; ./scripts/verify.sh passed lint, format, mypy, 155 unit tests and integration suite (3 passed, 10 skipped)"
    },
    {
      "id": "F017",
      "title": "[DEMO] Bootstrap vLLM embedding service",
      "behavior": "Добавляется GPU Compose profile с ai-sage/Giga-Embeddings-instruct-480M-0826, Hugging Face cache и OpenAI-compatible /v1/embeddings endpoint. Обычный CI может использовать contract stub; GPU verification запускается отдельно.",
      "state": "pass",
      "depends_on": [
        "F004"
      ],
      "verification": "pytest -q tests/unit/embeddings && pytest -q -m gpu tests/integration/test_vllm_embeddings.py",
      "evidence": "Independent verification iteration 2 passed all criteria; focused F017 tests: 22 passed, 1 safely skipped without GPU opt-in; ./scripts/verify.sh: Ruff, format, mypy, 158 unit passed, integration 3 passed / 10 skipped / 1 GPU deselected; default, GPU-profile, and .env.example Compose configs passed; rendered rag-api URL remained http://vllm-embeddings:8000/v1 while host GPU verification uses a separate localhost:8001 URL; git diff --check passed"
    },
    {
      "id": "F018",
      "title": "[DEMO] Implement embedding client contract",
      "behavior": "Создаётся единый embedding client с timeout, error mapping и dimension validation. rag-api и будущий worker используют один endpoint и не загружают модель локально.",
      "state": "pass",
      "depends_on": [
        "F017"
      ],
      "verification": "pytest -q tests/unit/embeddings/test_client.py tests/integration/test_embedding_contract.py",
      "evidence": "Independent verification passed all F018 criteria; focused client/contract tests: 13 passed; ./scripts/verify.sh passed Ruff, format, mypy, 170 unit tests and integration suite (4 passed, 10 skipped, 1 GPU deselected); git diff --check passed; verified shared async single/batch client, order preservation, EmbeddingSettings wiring, stable error mapping, strict response/dimension validation, local compatible stub contract and no local model loading"
    },
    {
      "id": "F019",
      "title": "[DEMO] Implement minimal OpenAI-compatible LLM adapter",
      "behavior": "Создаётся OpenAI-compatible client с base_url, model, secret, timeout и стабильным error mapping. Одна модель может обслуживать extraction и generation. Integration test использует локальный compatible stub.",
      "state": "pass",
      "depends_on": [
        "F004"
      ],
      "verification": "pytest -q tests/unit/llm/test_client.py tests/integration/test_llm_compatible_api.py",
      "evidence": "Independent verification iteration 3 passed all F019 and Ollama criteria; host defaults use http://localhost:11434/v1 and qwen3:1.7b, while Compose resolves rag-api to http://host.docker.internal:11434/v1 with a narrowly scoped host-gateway mapping; live Ollama /api/tags confirmed qwen3:1.7b and /v1/chat/completions returned non-empty assistant content with finish_reason=stop; focused tests passed; ./scripts/verify.sh passed Ruff, format, mypy, 186 unit tests and integration suite (5 passed, 10 skipped, 1 GPU deselected); production permits an absent Ollama key but rejects a supplied weak provider key; docker compose config and git diff --check passed"
    },
    {
      "id": "F020",
      "title": "[DEMO] Wire LightRAG Core to storages and model clients",
      "behavior": "Добавляется pinned LightRAG Core dependency. LightRAG настраивается на PostgreSQL-backed internal storage, Qdrant, Neo4j, embedding client и external LLM. Integration test доказывает минимальный insert/query на реальных storage.",
      "state": "pass",
      "depends_on": [
        "F015",
        "F016",
        "F018",
        "F019"
      ],
      "verification": "pytest -q tests/unit/rag/test_factory.py tests/integration/test_lightrag_storage_wiring.py",
      "evidence": "Independent verification iteration 4 passed all F020 criteria. Focused tests: 12 passed, 1 Docker-skipped; full verification: Ruff, format, mypy, 198 unit passed, integration 5 passed/11 skipped/1 deselected; Compose config and git diff checks passed. Pinned LightRAG 1.5.7 wiring covers PostgreSQL, Qdrant, Neo4j, shared embedding and external LLM clients. Prior Docker-backed verification proved real insert/query and graph persistence. Repeated-cancellation and cleanup-timeout probes confirm owned clients close, cleanup tasks are drained without orphans, and the original CancelledError is preserved."
    },
    {
      "id": "F021",
      "title": "[DEMO] Implement workspace-scoped LightRAG runtime registry",
      "behavior": "LightRAGRuntimeRegistry создаёт runtime по server-generated workspace.storage_key после получения AuthorizedWorkspaceContext. Реализуются lazy initialization и graceful close. Client не может выбрать physical namespace напрямую.",
      "state": "pass",
      "depends_on": [
        "F008",
        "F009",
        "F020"
      ],
      "verification": "pytest -q tests/unit/rag/test_runtime_registry.py tests/integration/test_runtime_workspace_isolation.py",
      "evidence": "Independent verification iteration 1 passed all criteria. Registry API accepts only AuthorizedWorkspaceContext and derives the physical namespace from server-generated storage_key. Focused tests: 6 passed, covering lazy initialization, concurrent singleton creation, retry after failure/cancellation, workspace isolation, idempotent close, and get/close races. Adversarial probes confirmed close-waiter cancellation safety, close failure semantics, no pending task leaks, and no raw namespace argument. Full verification passed Ruff, format, mypy, 203 unit tests and 6 integration tests (11 skipped, 1 deselected); git diff check passed."
    },
    {
      "id": "F022",
      "title": "[DEMO] Implement reusable inline IndexingService",
      "behavior": "IndexingService читает source, выбирает parser, переводит document в INDEXING, вызывает LightRAG insert и завершает READY/FAILED. Сервис не зависит от FastAPI request objects, чтобы позднее использоваться rag-worker без переписывания core logic.",
      "state": "pass",
      "depends_on": [
        "F013",
        "F014",
        "F021"
      ],
      "verification": "pytest -q tests/unit/indexing/test_service.py tests/integration/test_inline_indexing.py",
      "evidence": "Independent verification iteration 2 passed all F022 criteria. Focused tests: 10 passed. Full verification passed Ruff, format, mypy, 213 unit tests and integration suite (7 passed, 11 skipped, 1 deselected). Adversarial cancellation probes confirmed that cancellation after INDEXING makes a cancellation-safe FAILED transition, preserves the original CancelledError despite cleanup failure or repeated cancellation, and leaves no orphan tasks. The service reads through DocumentSourceService, selects parsers through ParserRegistry, obtains runtimes only from AuthorizedWorkspaceContext via LightRAGRuntimeRegistry, and has no FastAPI dependency. git diff --check passed."
    },
    {
      "id": "F023",
      "title": "[DEMO] Expose document upload/read API",
      "behavior": "POST /v1/workspaces/{id}/documents работает в trusted runtime без credentials: создаёт document metadata, сохраняет source и запускает inline IndexingService. GET list/get возвращают состояние. Для Demo допускается длительный HTTP request.",
      "state": "pass",
      "depends_on": [
        "F009",
        "F012",
        "F022"
      ],
      "verification": "pytest -q tests/unit/api/test_documents_demo.py tests/integration/test_document_upload_demo.py",
      "evidence": "Independent verification iteration 2 passed all F023 criteria. Focused F023/F022 lifecycle tests: 25 passed. Full scripts/verify.sh gate passed Ruff, format (81 files), mypy (38 files), and all configured unit/integration/e2e checks; git diff --check passed. POST persists server-generated metadata/source and runs inline indexing to READY, while failures durably preserve FAILED. GET list/item are workspace-scoped with safe 404 semantics. Filename validation rejects POSIX and Windows traversal/absolute forms, and metadata commit failures roll back and compensate object storage without masking the original error."
    },
    {
      "id": "F024",
      "title": "[DEMO] Implement Graph RAG query API with minimal sources",
      "behavior": "POST /v1/workspaces/{id}/query в trusted runtime разрешает workspace namespace, выполняет LightRAG retrieval/generation и возвращает answer, workspace_id, request_id и минимум document_id/filename sources. Query path не зависит от users, memberships, JWT или credentials.",
      "state": "pass",
      "depends_on": [
        "F021",
        "F023"
      ],
      "verification": "pytest -q tests/unit/query/test_service.py tests/integration/test_query_api_demo.py",
      "evidence": "Independent verification iteration 1 passed all F024 criteria. The framework-independent QueryService resolves LightRAG only through AuthorizedWorkspaceContext, executes non-streaming retrieval/generation, and returns workspace-scoped READY document sources with document_id/filename. The exact demo-owner POST query route works without JWT, creates unique UUID request IDs, rejects namespace injection/invalid payloads, and maps runtime failures to a safe 502. Focused tests: 6 passed. Full scripts/verify.sh gate passed Ruff, format, mypy, 227 unit tests and integration suite (12 passed, 11 skipped, 1 deselected); git diff --check passed."
    },
    {
      "id": "F025",
      "title": "[DEMO] Verify full Graph RAG happy path end-to-end",
      "behavior": "E2E на чистом Compose создаёт workspace, загружает небольшой документ, дожидается READY, задаёт вопрос и проверяет ожидаемый факт и source document. Тест доказывает полноценную Demo functionality в доверенном контуре без credentials.",
      "state": "pass",
      "depends_on": [
        "F024"
      ],
      "verification": "pytest -q tests/e2e/test_demo_graph_rag.py",
      "evidence": "Independent verification iteration 1 passed all F025 criteria. The new HTTP E2E uses an isolated clean Compose project with ephemeral published ports and deterministic local OpenAI-compatible model endpoints, creates a workspace, uploads a small document, waits for READY, queries the expected fact, and asserts the uploaded document_id/filename source without OIDC, IdP, JWT, or credentials. The full project verification gate passed (227 unit tests; integration 12 passed, 11 skipped, 1 deselected). The E2E itself skips only because this sandbox denies access to the Docker daemon; Compose/config/API/LightRAG wiring and cleanup were independently validated."
    },
    {
      "id": "F026",
      "title": "[DEMO] Provide deterministic init and Demo regression gate",
      "behavior": "Создаётся ./scripts/init.sh: prerequisite checks, Compose start, migrations и readiness checks. Повторный запуск идемпотентен. scripts/verify-demo.sh запускает обязательные Demo checks. README содержит copy-paste команды workspace/upload/query и явно описывает owner-only limitation.",
      "state": "pass",
      "depends_on": [
        "F025"
      ],
      "verification": "./scripts/verify-demo.sh",
      "evidence": "Independent verification iteration 3 passed all F026 criteria. scripts/init.sh is executable and idempotently validates Docker/Compose, preserves existing .env, starts the stack, applies Alembic migrations, ensures the MinIO bucket, and performs API, embedding, and LLM readiness probes before declaring Demo ready. scripts/verify-demo.sh runs unit, integration, and the exact F025 Demo E2E gates. README provides copy-paste unauthenticated workspace/upload/query commands and an explicit owner-only limitation. Mutation-sensitive model readiness tests cover both awaited probes and separate embedding/LLM failures. Full verification passed with 236 unit tests and 12 integration tests; the Docker-dependent E2E skipped only because the sandbox cannot access the Docker daemon."
    },
    {
      "id": "F027",
      "title": "[MVP] Add immutable document revisions",
      "behavior": "Модель документов расширяется document_revisions, stable document_id, monotonically increasing revision, content_hash, immutable object key и active_revision. Existing Demo rows безопасно мигрируются в revision 1.",
      "state": "pass",
      "depends_on": [
        "F026"
      ],
      "verification": "pytest -q tests/unit/documents/test_revisions.py tests/integration/test_document_revision_schema.py",
      "evidence": "Independent verification iteration 1 passed all F027 criteria. Documents now keep a stable document_id with an active_revision projection over immutable document_revisions rows keyed by (document_id, revision). Repository writes serialize revision allocation with a document-row lock, object URIs are revision-specific and unique, and a PostgreSQL trigger rejects revision updates. Alembic 0004 backfills every existing Demo document into revision 1 while preserving content hash, object URI, and timestamps, and provides a compatible downgrade projection. The mandatory gate passed with 3 tests and 1 Docker-dependent skip; the full verification pipeline passed Ruff, format, mypy, 240 unit tests and 12 integration tests. Live PostgreSQL schema tests skipped only because the sandbox cannot access the Docker daemon; offline Alembic DDL generation and migration inspection passed."
    },
    {
      "id": "F028",
      "title": "[MVP] Persist durable indexing jobs",
      "behavior": "Создаётся jobs state machine PENDING, RUNNING, RETRY, SUCCEEDED, FAILED, CANCELLED с attempts, lease/heartbeat, timestamps и safe error metadata. Для одного document запрещены конфликтующие active mutation jobs.",
      "state": "pass",
      "depends_on": [
        "F027"
      ],
      "verification": "pytest -q tests/unit/jobs/test_job_model.py tests/integration/test_job_constraints.py",
      "evidence": "Independent verification iteration 3 passed all F028 criteria after closing domain/database consistency and regression-coverage gaps. Jobs persist the explicit PENDING/RUNNING/RETRY/SUCCEEDED/FAILED/CANCELLED lifecycle against immutable document revisions, with mirrored attempts, lease/heartbeat, timestamp, bounded error-code, and lease-owner constraints. PostgreSQL enforces one active mutation job per document via a partial unique index while terminal jobs release the slot. The required gate passed with 43 tests and 1 Docker-dependent skip; the full verification pipeline passed Ruff, formatting, mypy, 281 unit tests and the integration suite (14 passed, 13 skipped, 1 deselected). Offline Alembic DDL and diff checks passed; the live PostgreSQL constraint probes skipped only because Docker is unavailable."
    },
    {
      "id": "F029",
      "title": "[MVP] Implement PostgreSQL job claiming and rag-worker",
      "behavior": "Добавляется отдельный rag-worker process/container. Jobs claim через FOR UPDATE SKIP LOCKED или эквивалент. Worker вызывает существующий IndexingService, поддерживает heartbeat, graceful shutdown, retry и expired lease recovery.",
      "state": "pass",
      "depends_on": [
        "F022",
        "F028"
      ],
      "verification": "pytest -q tests/unit/worker tests/integration/test_job_claiming.py tests/integration/test_worker_recovery.py",
      "evidence": "Independent verification iteration 3 passed all F029 criteria after resolving READY-before-job-success replay, shutdown-during-recovery, and lease-loss processing races. The separate rag-worker atomically claims jobs with PostgreSQL FOR UPDATE SKIP LOCKED, maintains owner-fenced leases and heartbeats, invokes the existing IndexingService for the exact revision, retries safely, recovers expired work, and drains gracefully. A document-scoped PostgreSQL advisory lock serializes external mutation across attempts, while exact job/revision/owner/attempt/live-lease fences reject stale document transitions. The focused gate passed with 11 tests and 3 Docker-dependent skips; the full verification pipeline passed Ruff, formatting, mypy, 288 unit tests and 18 integration tests. Docker Compose validation and diff checks passed; live PostgreSQL tests skipped only because the Docker daemon is unavailable."
    },
    {
      "id": "F030",
      "title": "[MVP] Switch upload from inline indexing to durable jobs",
      "behavior": "Public upload path сохраняет immutable source revision и атомарно создаёт indexing job, затем возвращает PENDING/job_id. Inline path может остаться только dev option. Runtime остаётся trusted single-owner и не принимает credentials.",
      "state": "pass",
      "depends_on": [
        "F027",
        "F029"
      ],
      "verification": "pytest -q tests/unit/api/test_documents_async.py tests/integration/test_document_upload_async.py",
      "evidence": "Independent verification iteration 3 passed all F030 criteria after correcting stale inline-upload documentation. Public upload now stores the immutable source revision and exact durable PENDING indexing job before one PostgreSQL commit, returns revision/status/job_id without storage metadata, and no longer invokes IndexingService inline. Failure rolls back database writes and best-effort deletes the unreferenced source object; DemoOwner DOCUMENT_CREATE authorization remains unchanged. Focused tests passed (3), upload/jobs/worker regression passed (63 passed, 3 skipped), and the full pipeline passed Ruff, formatting, mypy, 289 unit tests and 20 integration tests (16 skipped, 1 deselected); e2e skipped because Docker/external services were unavailable. git diff --check passed."
    },
    {
      "id": "F031",
      "title": "[MVP] Make identical document upsert idempotent",
      "behavior": "Под per-document transaction/lock одинаковый SHA-256 возвращает action=unchanged и не создаёт revision/job. Конкурентные identical upsert приводят к одной фактической mutation.",
      "state": "pass",
      "depends_on": [
        "F030"
      ],
      "verification": "pytest -q tests/unit/workflows/test_document_noop.py tests/integration/test_document_upsert_concurrency.py",
      "evidence": "Independent verification iteration 1 passed all F031 criteria. Public PUT upsert computes SHA-256 under a transaction-scoped PostgreSQL advisory lock, returns action=unchanged with job_id=null for identical content, and creates no source object, revision, or job on the no-op path. Concurrent identical requests are serialized to exactly one created mutation and one unchanged response; changed content returns 409 without mutation until F032. DemoOwner/workspace isolation and F030 atomic rollback semantics remain intact. The focused gate passed with 2 tests and 1 Docker-dependent skip; the full verification pipeline passed Ruff, formatting, mypy, 293 unit tests and 20 integration tests (17 skipped, 1 deselected), with e2e skipped because Docker is unavailable. git diff --check passed."
    },
    {
      "id": "F032",
      "title": "[MVP] Replace changed document through LightRAG lifecycle",
      "behavior": "Changed content создаёт новую revision, документ переходит в UPDATING, старая версия удаляется через supported LightRAG operation, новая индексируется, active_revision переключается только после успеха. Application не пишет vectors/graph entities напрямую.",
      "state": "pass",
      "depends_on": [
        "F031"
      ],
      "verification": "pytest -q tests/unit/workflows/test_document_replace.py tests/integration/test_document_replace.py",
      "evidence": "Independent verification iteration 4 passed all F032 criteria. Changed PUT atomically persists the next immutable revision and durable PENDING job, returns action=updated, moves the document to UPDATING, and keeps the prior revision active until success. The worker claims the real PENDING job, uses supported LightRAG delete-before-insert operations for the exact replacement revision, then atomically activates revision 2, sets READY, and completes the job under owner/attempt/live-lease fences; failures and stale workers cannot activate it. No direct application writes to vector or graph entities were introduced. The focused Docker gate passed 4 tests, migration/job-constraint checks passed 4 tests, the full integration suite passed 39 tests (1 deselected), the full unit suite passed 294 tests, and Ruff, formatting, mypy, and git diff checks passed."
    },
    {
      "id": "F033",
      "title": "[MVP] Implement document deletion workflow",
      "behavior": "DELETE создаёт durable job, переводит document в DELETING, вызывает LightRAG deletion и после подтверждения фиксирует DELETED. Source revisions сохраняются согласно простой retention policy.",
      "state": "pass",
      "depends_on": [
        "F032"
      ],
      "verification": "pytest -q tests/unit/workflows/test_document_delete.py tests/integration/test_document_delete.py",
      "evidence": "Independent verification iteration 2 passed all F033 criteria. Public DemoOwner DELETE atomically creates a durable DELETE_DOCUMENT job and moves READY documents to DELETING, preserves workspace isolation, and provides explicit idempotent DELETING/DELETED semantics. The production worker claims the real PENDING job, processes it under the document advisory lock, calls only supported LightRAG deletion for the exact active revision, and atomically sets DELETED plus SUCCEEDED under job type, document/revision, owner, attempt, and live-lease fences. Failures and stale workers do not prematurely delete the document; immutable revision rows and source objects are retained under the documented MVP policy. The exact focused gate passed 6 tests, related API/F031/F032 regressions passed 22 tests, the full unit suite passed 301 tests, and Ruff, formatting, mypy, and git diff checks passed. Migration 20260918_0007 and its DELETING/DELETED and DELETE_DOCUMENT constraints were exercised. Broad integration infrastructure errors were unrelated Docker port/startup and external Qdrant timeout failures; the exact F033 Docker gate passed."
    },
    {
      "id": "F034",
      "title": "[MVP] Preserve recoverable state on workflow failures",
      "behavior": "Ошибки parser, embedding, LLM, Qdrant, Neo4j или LightRAG не активируют неподтверждённую revision. Job получает RETRY/FAILED, а source и metadata сохраняются для безопасного повторного исполнения.",
      "state": "pass",
      "depends_on": [
        "F029",
        "F032",
        "F033"
      ],
      "verification": "pytest -q tests/unit/workflows/test_failures.py tests/integration/test_indexing_failure_recovery.py",
      "evidence": "Independent verification iteration 2 passed all F034 criteria. Durable CREATE retains immutable revision/source/job metadata with active_revision=NULL until the exact leased worker attempt successfully reaches READY; UPDATE failures preserve the prior active revision, and DELETE failures remain non-DELETED. Parser, embedding, LLM, Qdrant, Neo4j, and LightRAG-stage failures execute through the real indexing/worker path, transition the exact owner+attempt+live-lease job through bounded RETRY to FAILED, reject stale attempts, and retain recoverable metadata. The exact live Docker/PostgreSQL gate passed 9 tests; migration and F030-F033 regressions passed 24 tests; Ruff, formatting, mypy, git diff checks, 308 unit tests, and 42 integration tests passed. The remaining E2E failure was independently diagnosed as pre-existing Compose sequencing: rag-worker starts before Alembic creates graph_blizz.jobs and exits; no F034 change touches that startup path."
    },
    {
      "id": "F035",
      "title": "[MVP] Recover in-flight workflows after restart",
      "behavior": "После restart rag-api/rag-worker/PostgreSQL expired RUNNING jobs могут быть продолжены без duplicate revision, duplicate logical document или параллельной mutation.",
      "state": "pass",
      "depends_on": [
        "F034"
      ],
      "verification": "pytest -q tests/integration/test_workflow_restart_recovery.py",
      "evidence": "Independent verification iteration 3 passed all F035 criteria. The exact live gate stopped and restarted PostgreSQL, recovered the same expired RUNNING job, preserved the original job/document/revision identities with one row each, allowed exactly one of two replacement workers to execute, and rejected stale owner/attempt completion. Compose now restarts rag-worker unless explicitly stopped. Test-only Compose teardown uses a short down timeout while production retains its two-minute graceful-stop period. The exact gate passed 2 tests; the full verification pipeline passed Ruff, formatting, mypy, 308 unit tests, 44 integration tests (1 deselected), and 1 E2E test with no leaked verification projects or port conflicts."
    },
    {
      "id": "F036",
      "title": "[MVP] Add Tree-sitter AST parsing foundation",
      "behavior": "Basic parser registry расширяется Tree-sitter adapters для фиксированного начального набора, например Python, JavaScript/TypeScript/TSX, Java и Go. Syntax errors имеют deterministic fallback; бинарные форматы отклоняются.",
      "state": "pass",
      "depends_on": [
        "F014",
        "F026"
      ],
      "verification": "pytest -q tests/unit/parsers/test_tree_sitter.py tests/integration/test_ast_parsers.py",
      "evidence": "Independent verification iteration 2 passed all F036 criteria. The registry selects Tree-sitter adapters for Python, JavaScript, TypeScript, TSX, Java, and Go while preserving MIME precedence; all six grammars execute through Tree-sitter. Malformed syntax produces a deterministic documented single-chunk fallback, and known binary MIME types are rejected before misleading code-extension fallback. The exact gate passed 8 tests, an 18-type binary MIME matrix was rejected, parser/indexing/upload regressions passed 58 tests, and the full pipeline passed Ruff, formatting, mypy, 318 unit tests, and 27 integration tests (23 skipped, 1 deselected); the E2E test was skipped because Docker was unavailable."
    },
    {
      "id": "F037",
      "title": "[MVP] Produce semantic code chunks",
      "behavior": "Source code делится по module/class/function/method boundaries; слишком крупные symbols дополнительно режутся по token limit. Каждый chunk сохраняет path, language, symbol, symbol_type, parent_symbol и start/end line.",
      "state": "pass",
      "depends_on": [
        "F036"
      ],
      "verification": "pytest -q tests/unit/parsers/test_code_chunking.py",
      "evidence": "Independent verification iteration 2 passed all F037 criteria. Semantic AST chunking is deterministic for Python, JavaScript, TypeScript/TSX, Java, and Go; oversized regions respect the explicit token limit; required path/language/symbol hierarchy and inclusive line metadata are preserved as strings; ordered chunks reconstruct Unicode source losslessly; and the F036 syntax-error fallback remains compatible. Exact gate: 10 passed; focused integration: 6 passed; full verification: Ruff, formatting, mypy, 328 unit tests, and 27 integration tests passed. Infrastructure-only Docker/GPU scenarios were skipped by the standard gates."
    },
    {
      "id": "F038",
      "title": "[MVP] Preserve rich source provenance through retrieval",
      "behavior": "Query response для source code возвращает document_id, revision, path, language, symbol и line ranges, когда они доступны. Metadata проходит parser -> indexing -> retrieval без потери document identity.",
      "state": "pass",
      "depends_on": [
        "F032",
        "F037"
      ],
      "verification": "pytest -q tests/unit/query/test_sources.py tests/integration/test_query_source_traceability.py",
      "evidence": "Independent verification iteration 2 passed all F038 criteria. Source-code parser metadata is encoded into deterministic LightRAG chunk documents and returned as document_id, revision, path, language, symbol, start_line, and end_line when available. Query validates citations against workspace-scoped READY documents at the active revision while preserving legacy and non-code behavior. Replacement indexing uses the target job revision and is covered with actual Tree-sitter chunks. Exact gate: 2 passed; focused regressions: 23 passed, 4 infrastructure-dependent skipped; full verification: Ruff, formatting, mypy, 330 unit tests, and 28 integration tests passed; infrastructure-only E2E was skipped."
    },
    {
      "id": "F039",
      "title": "[MVP] Prove multi-workspace data isolation",
      "behavior": "E2E создаёт workspace A и B в одном trusted runtime, загружает разные знания и доказывает, что query A не возвращает B и наоборот. Проверяется actual isolation PostgreSQL/LightRAG/Qdrant/Neo4j независимо от user ACL.",
      "state": "pass",
      "depends_on": [
        "F030",
        "F038"
      ],
      "verification": "pytest -q tests/e2e/test_workspace_data_isolation.py",
      "evidence": "Independent verification iteration 1 passed all F039 criteria with no issues. The E2E creates two workspaces under one Demo Owner through public APIs, indexes distinct facts, proves bidirectional answer/source isolation, and directly validates physical partitioning in PostgreSQL, LightRAG, Qdrant, and Neo4j. Exact gate was infrastructure-skipped because Docker daemon is unavailable; Ruff, format, mypy, git diff check, and the full available pipeline passed with 330 unit and 28 integration tests; 2 Docker-dependent E2E tests skipped."
    },
    {
      "id": "F040",
      "title": "[MVP] Add parser and index schema versioning",
      "behavior": "Workspace хранит index_schema_version и embedding profile identity; revisions фиксируют parser/chunk/index versions. Несовместимое изменение помечает данные requiring reindex вместо смешивания несовместимых индексов.",
      "state": "pass",
      "depends_on": [
        "F027",
        "F037"
      ],
      "verification": "pytest -q tests/unit/indexing/test_schema_version.py tests/integration/test_index_versioning.py",
      "evidence": "Independent verification iteration 4 passed all F040 criteria with no blocker or major issues. Workspaces persist index schema and canonical embedding-profile identity; revisions persist actual parser/chunk/index versions; contract changes serialize on the workspace row, atomically mark active non-deleted revisions for reindex, and select schema/profile-isolated LightRAG runtimes. Migration 0009 safely backfills Demo rows and renders correct offline upgrade/downgrade SQL. Exact gate passed with 4 tests and 3 Docker-only skips; focused checks passed with 19 tests and 5 Docker-only skips; full verification passed Ruff, format, mypy, 331 unit tests and 30 integration tests; 2 Docker-dependent E2E tests skipped."
    },
    {
      "id": "F041",
      "title": "[MVP] Implement durable workspace reindex",
      "behavior": "Maintenance operation создаёт durable jobs и перестраивает derived indexes из PostgreSQL metadata и MinIO revisions. Reindex переживает restart и не требует повторной загрузки originals.",
      "state": "pass",
      "depends_on": [
        "F035",
        "F040"
      ],
      "verification": "pytest -q tests/unit/maintenance/test_reindex.py tests/e2e/test_workspace_reindex.py",
      "evidence": "Independent verification iteration 2 passed all F041 criteria with no issues. The maintenance operation reconciles the active index contract and enqueues durable reindex jobs in one transaction; workers recover PostgreSQL revision metadata and persisted MinIO originals, survive retry/restart through durable leases, and atomically mark jobs successful, update provenance, and clear requires_reindex only after indexing. Failures preserve recoverable state. Required gate: 5 passed, 1 Docker-only skip; full verification passed Ruff, formatting, mypy, 336 unit tests and 30 integration tests; git diff, compileall, and single-head Alembic history checks passed."
    },
    {
      "id": "F042",
      "title": "[MVP] Verify full single-owner lifecycle and persistence",
      "behavior": "Full-stack E2E выполняет create, READY, unchanged upsert, changed revision, query v2, delete, restart stack и повторную проверку persisted source-of-truth state. Тест работает в trusted no-credentials runtime.",
      "state": "pass",
      "depends_on": [
        "F033",
        "F035",
        "F039",
        "F041"
      ],
      "verification": "pytest -q tests/e2e/test_mvp_lifecycle.py tests/e2e/test_full_stack_restart.py",
      "evidence": "Independent verification iteration 2 passed every F042 criterion and resolved F042-V1/V2/V3. The live Compose gate passed 2 tests: public create-to-READY, unchanged upsert, revision-2 update, v2-only query, delete, full service restart, and persisted PostgreSQL/MinIO/API/query state. The harness uses isolated projects, ephemeral ports with post-restart re-resolution, deterministic model services, bounded readiness barriers, and infrastructure-only skips. Ruff, formatting, mypy, 336 unit tests, git diff checks, and the required live gate passed; unrelated pre-existing integration fixture, migration-head, uniqueness, and external registry failures remain outside F042."
    },
    {
      "id": "F043",
      "title": "[REST-READY] Publish Neo4j HTTP and Bolt ports",
      "behavior": "Docker Compose публикует Neo4j Browser/HTTP 7474 и Bolt 7687 через настраиваемые GRAPH_BLIZZ_NEO4J_HTTP_PORT и GRAPH_BLIZZ_NEO4J_BOLT_PORT. rag-api и rag-worker продолжают использовать внутренний bolt://neo4j:7687. README описывает адреса, credentials и ограничение доверенной сетью.",
      "state": "pass",
      "depends_on": [
        "F042",
        "F060"
      ],
      "verification": "docker compose config && pytest -q tests/unit/test_compose_web_access.py tests/integration/test_neo4j_bootstrap.py",
      "evidence": "Independent verification iteration 1 passed all F043 criteria with no issues. Neo4j publishes configurable Browser/HTTP and Bolt host ports while rag-api and rag-worker keep the internal bolt://neo4j:7687; PostgreSQL stays internal-only and MinIO/Qdrant/rag-api port contracts are unchanged. Focused gate passed: Compose config rendered successfully and 6 focused tests passed with the Docker-only Neo4j integration test skipped; an independent mutation check proved the unit assertions fail when the ports or the host_access membership are removed. README, .env.example, ADR 0008, architecture section 5.7 and CHANGELOG are consistent. No Docker daemon was available, so live reachability of the published host ports was not exercised."
    },
    {
      "id": "F044",
      "title": "[REST-READY] Use only an existing external vLLM embedding endpoint",
      "behavior": "Из Docker Compose удаляются vllm-embeddings, GPU profile, NVIDIA reservation и Hugging Face cache. rag-api и rag-worker получают одинаковые base_url/model/dimension/timeout/API key внешнего OpenAI-compatible vLLM. init проверяет endpoint, но не запускает модель; tests используют contract stub или явно заданный live endpoint.",
      "state": "pass",
      "depends_on": [
        "F018",
        "F026",
        "F042"
      ],
      "verification": "docker compose config && pytest -q tests/unit/embeddings tests/integration/test_embedding_contract.py tests/unit/test_demo_scripts.py",
      "evidence": "Independent verification iteration 1 passed all F044 criteria with no issues. Docker Compose no longer declares vllm-embeddings, the gpu profile, NVIDIA reservation or the Hugging Face cache, and rag-api and rag-worker receive identical external OpenAI-compatible embedding settings (base_url/model/dimension/timeout plus an optional API key that stays unset, not blank, when absent); init.sh probes the configured embedding and LLM endpoints without starting a model. Focused gate passed (docker compose config; 30 tests), targeted regressions 12 passed, 339 unit tests, ruff/format/mypy and git diff --check passed, and mutation checks proved the external-only Compose assertions fail when the bundled service or the Compose-DNS default is restored. No Docker daemon was available, so live external endpoint reachability was not exercised."
    },
    {
      "id": "F045",
      "title": "[REST-READY] Persist workspace descriptions",
      "behavior": "Новая Alembic migration добавляет nullable workspaces.description. Create принимает необязательное нормализованное описание длиной до 2000 символов; repository/entity и create/get responses возвращают его. Существующие строки безопасно получают NULL, storage_key остаётся непубличным.",
      "state": "pass",
      "depends_on": [
        "F008",
        "F042"
      ],
      "verification": "pytest -q tests/unit/workspaces/test_repository.py tests/unit/api/test_workspaces_demo.py tests/integration/test_workspace_schema.py tests/integration/test_workspace_api_demo.py",
      "evidence": "Independent verification iteration 1 passed all 8 F045 criteria with no blocker/major issues; only minor environmental notes remain (no Docker daemon for a live PostgreSQL apply; pre-existing stale REVISION pin in tests/integration/test_migrations.py, out of F045 scope). Single-head Alembic revision 20260924_0011 (down_revision 20260921_0010) adds a nullable TEXT description to graph_blizz.workspaces and drops it on downgrade (confirmed by alembic heads/history and offline upgrade/downgrade SQL); the Workspace entity, shared projection/insert and create/get responses carry it; surrounding whitespace is trimmed, blank/whitespace-only normalizes to null, 2000 chars are accepted and 2001 rejected with 422, and pre-migration rows map to null; storage_key and embedding_profile stay private and extra=forbid still rejects unknown fields. Focused gate 19 passed / 3 Docker skips (baseline 9 passed / 2 skipped); ./scripts/verify.sh exit 0 with 349 unit tests; ruff/format, mypy and git diff --check clean; mutation probes proved the new tests are non-vacuous (reverting repository persistence, API acceptance or trimming each fails them)."
    },
    {
      "id": "F046",
      "title": "[REST-READY] Expose workspace list API",
      "behavior": "GET /workspaces возвращает все workspace с id, name, slug, description, status и timestamps в детерминированном порядке created_at ASC, id ASC. Endpoint не инициализирует LightRAG runtime, не раскрывает storage_key/index profile и работает без Authorization header.",
      "state": "pass",
      "depends_on": [
        "F045"
      ],
      "verification": "pytest -q tests/unit/api/test_workspaces_demo.py tests/integration/test_workspace_api_demo.py",
      "evidence": "GET /workspaces (router prefix /workspaces, path \"\") -> 200 list[WorkspaceResponse]; public fields only (no storage_key/embedding_profile/index_schema_version); WorkspaceRepository.list() SQL ORDER BY created_at, id; works without Authorization header; app.state.runtime_registry untouched. pytest unit+integration workspace API 14 passed; ./scripts/verify.sh exit 0 (ruff/mypy clean, 352 unit passed, 31 integration passed + 28 skipped, e2e 5 skipped). Independent verify iteration 1: all 8 criteria pass incl. mutation probes (wrong order, leaked storage_key, required auth, runtime lookup each fail the suite)."
    },
    {
      "id": "F047",
      "title": "[REST-READY] Expose durable job status API",
      "behavior": "Добавляется GET /v1/jobs/{job_id}, возвращающий публичные id, type, status, workspace_id, document_id, document_revision, attempt, created_at и updated_at без lease owner, storage metadata и exception details. Endpoint нужен REST- и MCP-клиентам для polling асинхронных upload/update/delete/reindex operations.",
      "state": "not started",
      "depends_on": [
        "F028",
        "F042"
      ],
      "verification": "pytest -q tests/unit/api/test_jobs.py tests/integration/test_job_status_api.py",
      "evidence": ""
    },
    {
      "id": "F048",
      "title": "[REST-READY] Freeze and verify the REST contract for MCP",
      "behavior": "OpenAPI и regression tests фиксируют workspace description/list, public job status и существующие document/query contracts. Полный lifecycle проходит с внешним embedding contract stub или явно настроенным vLLM, без локального model service и без Authorization header. Architecture, README и example commands синхронизируются до начала MCP.",
      "state": "not started",
      "depends_on": [
        "F043",
        "F044",
        "F046",
        "F047"
      ],
      "verification": "./scripts/verify.sh && pytest -q tests/e2e/test_mvp_lifecycle.py tests/e2e/test_full_stack_restart.py",
      "evidence": ""
    },
    {
      "id": "F049",
      "title": "[MCP] Bootstrap MCP package and typed REST client",
      "behavior": "Создаётся отдельный graph-blizz-mcp entry point с stdio transport и настраиваемым GRAPH_BLIZZ_API_BASE_URL. Типизированный async client вызывает только public REST API, имеет bounded timeout, корректно закрывает соединения и преобразует HTTP/validation failures в стабильные MCP errors. MCP не импортирует repositories/storage adapters и не получает storage credentials.",
      "state": "not started",
      "depends_on": [
        "F048"
      ],
      "verification": "pytest -q tests/unit/mcp/test_client.py tests/unit/mcp/test_server_bootstrap.py",
      "evidence": ""
    },
    {
      "id": "F050",
      "title": "[MCP] Implement workspace discovery and query tools",
      "behavior": "MCP server предоставляет list_workspaces, get_workspace, list_documents, get_document и query_workspace. Tool schemas используют публичные REST fields; list_workspaces возвращает description, query_workspace сохраняет request_id и sources. Ни один tool не принимает storage_key или произвольный downstream URL.",
      "state": "not started",
      "depends_on": [
        "F049"
      ],
      "verification": "pytest -q tests/unit/mcp/test_read_tools.py tests/integration/test_mcp_query_tools.py",
      "evidence": ""
    },
    {
      "id": "F051",
      "title": "[MCP] Implement document mutation and reindex tools",
      "behavior": "Добавляются upload_document, upsert_document, delete_document, reindex_workspace и get_job_status. Mutating tools вызывают только соответствующие REST endpoints, передают filename/media type/content в ограниченном контракте и немедленно возвращают action/revision/job_id/status; ожидание завершения выполняется отдельным polling tool через public job API.",
      "state": "not started",
      "depends_on": [
        "F050"
      ],
      "verification": "pytest -q tests/unit/mcp/test_mutation_tools.py tests/integration/test_mcp_mutation_tools.py",
      "evidence": ""
    },
    {
      "id": "F052",
      "title": "[MCP] Verify complete MCP-over-REST workflow",
      "behavior": "E2E запускает rag-api, rag-worker и MCP server, получает список workspace с descriptions, загружает документ через MCP, опрашивает public job status, выполняет query и проверяет answer/sources, затем удаляет документ. Тест доказывает отсутствие прямых MCP connections к PostgreSQL, MinIO, Qdrant и Neo4j. README содержит воспроизводимую stdio-конфигурацию клиента.",
      "state": "not started",
      "depends_on": [
        "F051"
      ],
      "verification": "pytest -q tests/e2e/test_mcp_lifecycle.py",
      "evidence": ""
    },
    {
      "id": "F053",
      "title": "[POST-MCP] Verify trusted network exposure",
      "behavior": "Automated test проверяет заявленную сетевую модель: rag-api, MinIO, Qdrant и Neo4j публикуют только документированные configurable ports; PostgreSQL и rag-worker остаются internal-only; embedding vLLM не управляется Compose. README фиксирует, что опубликованные storage ports предназначены только для trusted network.",
      "state": "not started",
      "depends_on": [
        "F043",
        "F052"
      ],
      "verification": "pytest -q tests/integration/test_network_boundaries.py",
      "evidence": ""
    },
    {
      "id": "F054",
      "title": "[POST-MCP] Harden secrets and external endpoint configuration",
      "behavior": "Secrets не попадают в git, config responses, MCP results или logs. Embedding и LLM URLs валидируются на уровне конфигурации; provider errors не раскрывают credentials, prompts, document content или полные response bodies. Политика учитывает, что embedding endpoint является внешней зависимостью.",
      "state": "not started",
      "depends_on": [
        "F044",
        "F052"
      ],
      "verification": "pytest -q tests/unit/config/test_settings.py tests/unit/observability/test_logging.py tests/unit/mcp/test_error_mapping.py",
      "evidence": ""
    },
    {
      "id": "F055",
      "title": "[POST-MCP] Add metrics and tracing",
      "behavior": "HTTP, jobs, retrieval, embeddings, LLM calls и MCP client calls instrumentируются metrics/tracing. request_id/trace_id связывают MCP operation с REST request и worker job. Документ content, prompts, embeddings и credentials не попадают в telemetry.",
      "state": "not started",
      "depends_on": [
        "F029",
        "F052"
      ],
      "verification": "pytest -q tests/unit/observability/test_otel.py tests/integration/test_otel_pipeline.py",
      "evidence": ""
    },
    {
      "id": "F056",
      "title": "[MCP] Add optional Streamable HTTP transport",
      "behavior": "После стабильного stdio MCP добавляется optional Streamable HTTP transport с теми же tool schemas и REST-backed implementation. Transport не вводит авторизацию, не получает storage credentials и может быть отключён одной настройкой.",
      "state": "not started",
      "depends_on": [
        "F052"
      ],
      "verification": "pytest -q tests/unit/mcp/test_http_transport.py tests/integration/test_mcp_http_transport.py",
      "evidence": ""
    },
    {
      "id": "F057",
      "title": "[POST-MCP] Verify disaster-recovery rebuild from source of truth",
      "behavior": "E2E очищает derived Qdrant/Neo4j data и выполняет reindex из PostgreSQL metadata и MinIO revisions через existing REST API. После rebuild Graph RAG и MCP query снова отвечают по исходным документам.",
      "state": "not started",
      "depends_on": [
        "F041",
        "F052"
      ],
      "verification": "pytest -q tests/e2e/test_disaster_recovery_reindex.py",
      "evidence": ""
    },
    {
      "id": "F058",
      "title": "[POST-MCP] Add deployment-readiness runtime contracts",
      "behavior": "Уточняются /health/ready semantics, SIGTERM/graceful shutdown, configurable worker concurrency, MCP process lifecycle и запрет critical local-filesystem state. Helm chart и user security не входят в задачу.",
      "state": "not started",
      "depends_on": [
        "F055"
      ],
      "verification": "pytest -q tests/unit/runtime tests/integration/test_readiness_and_shutdown.py",
      "evidence": ""
    },
    {
      "id": "F060",
      "title": "[DEMO] Expose web interfaces and FastAPI documentation",
      "behavior": "Docker Compose публикует rag-api с FastAPI Swagger/ReDoc и web console MinIO на настраиваемых host-портах. PostgreSQL остаётся internal-only, а README описывает адреса подключения.",
      "state": "pass",
      "depends_on": [
        "F001",
        "F011"
      ],
      "verification": "docker compose config && pytest -q tests/unit/test_app_bootstrap.py tests/unit/test_compose_web_access.py",
      "evidence": "Independent live Compose verification: Swagger /docs and /redoc returned HTTP 200; MinIO API health and Console returned HTTP 200 with effective host bindings; PostgreSQL remained unpublished/internal-only. Focused tests: 10 passed; ./scripts/verify.sh: lint, format, mypy, 110 unit tests and 3 integration tests passed (8 skipped)."
    },
    {
      "id": "F059",
      "title": "[POST-MCP] Establish final regression gate and synchronize documentation",
      "behavior": "Финальный scripts/verify.sh объединяет lint/typecheck, unit, integration, REST lifecycle, MCP lifecycle, recovery, logging и deployment tests. GPU tests и live external-model checks остаются отдельными opt-in checks. architecture.md, README и operational docs описывают trusted no-auth deployment, external embeddings, published Neo4j ports, workspace list/description и MCP-over-REST.",
      "state": "not started",
      "depends_on": [
        "F052",
        "F054",
        "F057",
        "F058"
      ],
      "verification": "./scripts/verify.sh",
      "evidence": ""
    }
  ]
}
